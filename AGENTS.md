# Teddy 项目 Codex 协作规则

本仓库是 Teddy 成人用品即时零售项目的统一资料库。任何 Codex 工作流开始前，必须先理解以下规则。

## 项目目录

- 本地推荐目录：`D:\Teddy创业项目`
- GitHub 仓库：`https://github.com/l596955124-hue/Teddy`
- 主线程：商业计划书和PPT总控
- 专项资料：统一放在 `工作流专区/`

## 开工前必读

每个 Codex 工作流开始前，先读取：

1. `项目总控上下文-精简版.md`
2. `跨工作流同步规则.md`
3. `项目协作规则.md`
4. `工作流专区/README.md`
5. 当前任务对应专项目录的最新文件

## 协作边界

- 商业计划书和PPT修改：放在 `工作流专区/01-商业计划书主线程/` 或根目录对应商业计划文件。
- 选品、供应商、SKU：放在 `工作流专区/02-选品与供应商专项/`。
- 岗位职责、薪酬、绩效、分红：放在 `工作流专区/03-岗位职责与薪酬分红专项/`。
- LOGO、VI、物料：过程资料放在 `工作流专区/04-品牌视觉资产专项/`，正式资产放在 `品牌资产/`。
- 平台入驻、规则、资质：放在 `工作流专区/05-平台入驻专项/`。

新增专项必须在 `工作流专区/` 下创建独立中文文件夹，避免资料混放。

## 保存规则

- 重要结论必须写入本地文件，不能只保存在聊天里。
- 阶段存档放入对应目录的 `阶段存档/`，或项目根目录 `阶段存档/`。
- 阶段存档命名：`YYYY-MM-DD-主题保存.md`。
- 如果影响项目总口径，必须同步更新 `项目总控上下文-精简版.md`。

## Git 规则

- 修改前先 `git pull`。
- 不提交 `.env`、API Key、Token、密码、`.firecrawl/`、`node_modules/`、`outputs/`。
- 多人协作时优先各自使用独立分支或独立工作流目录，确认后再合并到 `main`。
- 不要删除他人刚生成的文件，除非已经确认无用并获得项目负责人同意。

## 汇报规则

每个工作流完成阶段任务后，应输出：

1. 做了什么。
2. 保存到哪些文件。
3. 是否影响商业计划书/PPT主口径。
4. 是否需要主线程同步。
5. 是否有待确认问题。

项目文件优先于聊天记忆。如聊天内容与文件冲突，以最新且优先级更高的本地文件为准。
