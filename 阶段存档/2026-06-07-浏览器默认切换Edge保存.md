# 阶段存档：浏览器默认切换为Edge

保存时间：2026-06-07

## 已完成

- 已将项目协作规则更新为：后续网页操作默认使用 Microsoft Edge。
- 用户已在 Edge 登录淘宝、小红书、抖音、1688、京东，后续选品和平台调研优先使用 Edge 登录态。
- 已新增 Edge 调试启动脚本：

`D:\新疆\测试\商业计划\工具脚本\启动Edge调试浏览器.cmd`

## 当前技术状态

- 当前 Edge 正在普通模式运行。
- 尝试在不关闭现有 Edge 的情况下开启远程调试端口 `9223`，未成功连接。
- 判断原因：Edge 已有普通模式进程，新的远程调试启动参数被合并到已有进程，端口未生效。

## 后续使用方式

如果只是让用户手动打开网页，不需要额外操作。

如果需要 Codex 自动读取网页内容、抓取页面文本、截图分析或整理选品数据，需要：

1. 人工关闭所有 Edge 窗口。
2. 运行 `D:\新疆\测试\商业计划\工具脚本\启动Edge调试浏览器.cmd`。
3. 打开后继续使用已经登录的 Edge 账号环境。

## 默认规则

后续不默认使用 Chrome；除非 Edge 无法访问目标网站，或用户临时指定其他浏览器。
